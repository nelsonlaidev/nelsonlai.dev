pre-commit:
  parallel: true
  jobs:
    - name: lint-staged
      run: bun lint-staged

commit-msg:
  parallel: true
  jobs:
    - name: commitlint
      run: bun commitlint --edit {1}

    - name: spelling
      run: bun check:spelling {1}
