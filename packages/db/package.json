{
  "name": "@repo/db",
  "version": "0.0.0",
  "license": "MIT",
  "author": "Nelson Lai <me@nelsonlai.dev> (https://github.com/nelsonlaidev/)",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "clean": "rm -rf .turbo",
    "db:check": "bun with-env drizzle-kit check",
    "db:generate": "bun with-env drizzle-kit generate",
    "db:migrate": "bun with-env drizzle-kit migrate",
    "db:push": "bun with-env drizzle-kit push",
    "db:reset": "bun with-env bun run ./src/reset.ts",
    "db:seed": "bun with-env bun run ./src/seed.ts",
    "db:studio": "bun with-env drizzle-kit studio",
    "lint": "eslint . --max-warnings 0",
    "lint:fix": "eslint --fix .",
    "type-check": "tsc --noEmit",
    "with-env": "dotenvx run -f ../../.env.local --ignore=MISSING_ENV_FILE --"
  },
  "lint-staged": {
    "*.{js,mjs,jsx,ts,mts,tsx}": "eslint --fix",
    "*": [
      "prettier --write --ignore-unknown",
      "cspell --no-progress --no-summary --no-must-find-files --unique"
    ]
  },
  "dependencies": {
    "@paralleldrive/cuid2": "catalog:prod",
    "@repo/env": "workspace:*",
    "drizzle-orm": "catalog:prod"
  },
  "devDependencies": {
    "@nelsonlaidev/eslint-config": "catalog:lint",
    "@nelsonlaidev/typescript-config": "catalog:dev",
    "@types/node": "catalog:types",
    "consola": "catalog:prod",
    "drizzle-kit": "catalog:dev",
    "execa": "catalog:dev",
    "postgres": "catalog:prod"
  },
  "peerDependencies": {
    "postgres": ">=3.0.0"
  }
}
