{
  "name": "@repo/kv",
  "version": "0.0.0",
  "license": "MIT",
  "author": "Nelson Lai <me@nelsonlai.dev> (https://github.com/nelsonlaidev/)",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "clean": "rm -rf .turbo",
    "lint": "eslint . --max-warnings 0",
    "lint:fix": "eslint --fix .",
    "type-check": "tsc --noEmit"
  },
  "lint-staged": {
    "*.{js,mjs,jsx,ts,mts,tsx}": "eslint --fix",
    "*": [
      "prettier --write --ignore-unknown",
      "cspell --no-progress --no-summary --no-must-find-files --unique"
    ]
  },
  "dependencies": {
    "@upstash/ratelimit": "catalog:prod",
    "@upstash/redis": "catalog:prod",
    "consola": "catalog:prod"
  },
  "devDependencies": {
    "@nelsonlaidev/eslint-config": "catalog:lint",
    "@nelsonlaidev/typescript-config": "catalog:dev",
    "@repo/env": "workspace:*"
  }
}
